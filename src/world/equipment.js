// STUB — owned by agent-kiosk.
import * as THREE from 'three';
export function buildEquipment() { return { group: new THREE.Group(), colliders: [], interactables: [] }; }
export function getStationAnchors() { return {}; }
